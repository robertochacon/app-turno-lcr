import { SearchTurnPipe } from './search-turn.pipe';

describe('SearchTurnPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTurnPipe();
    expect(pipe).toBeTruthy();
  });
});
